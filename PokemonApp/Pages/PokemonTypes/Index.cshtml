@page
@model PokemonApp.Pages.PokemonTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PokemonType[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PokemonType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.PokemonTypeId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.PokemonTypeId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.PokemonTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
